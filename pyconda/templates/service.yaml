apiVersion: v1
kind: Service
metadata:
  name: {{ include "pyconda.fullname" . }}
  labels:
    {{- include "pyconda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pyconda.selectorLabels" . | nindent 4 }}
